#include <iostream>
#include <fstream>

 void filePull(std::string filename, vector & <Music> musicList ){

    ifstream musicFile;
    musicFile.open(filename);

    if (musicFile.is_open())

    {





    }








 }
